一、synchronized
	1、一个对象才new且调用了hashcode方法后（如果不调用hashcode方法，那么存放hashcode的31位全部为0），正常情况下处于无锁状态。
	2、如果在单线程下使用synchronized，并且是使用对象锁，那个这个时候就是轻量级锁。
	3、如果有出现锁竞争的时候，就会从轻量级锁膨胀到重量级锁，注意如果锁从轻量级锁膨胀到重量级锁后，时无法回退到轻量级锁的。
	
	
二、垃圾回收机制。
	